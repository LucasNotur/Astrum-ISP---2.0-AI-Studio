import { describe, it, expect } from 'vitest';
// import { syncErp } from '../../src/workers/erpSyncWorker';

describe('erpSyncWorker', () => {
  it('should sync erp data correctly', () => {
    expect(true).toBe(true);
  });
});
