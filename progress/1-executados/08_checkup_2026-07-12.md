# Checkup geral + saneamento do banco — ✅ (2026-07-12, commits 8414415 + c6f4959 + este)

**Fonte:** PROGRESS_LOG entradas de 2026-07-12.

## O que o checkup encontrou e corrigiu
1. **103 imports relativos quebrados** que impediam o BOOT real do motor v2
   (todos os 14 workers de `packages/queue`, 4 dynamic imports do `server.ts`,
   kb-draft, 15 arquivos de teste do legado). Testes passavam por mock; runtime não.
2. **39 erros de typecheck** no apps/api → 0. Incluía bugs reais: RBAC sem
   `service_orders` (técnico levaria 403 no D-06), `await` faltando no DuckDB,
   canais P2 fora do port de conversas, export inexistente no vision.service.
3. **Bug de schema latente:** `service_orders` sem `scheduled_for`/`created_by`/
   `external_id` e CHECK de status só em pt-BR — INSERTs do P3/P4/tools quebrariam
   em produção → migration 073.
4. **Banco local Supabase migrado e verificado contra o código** (2026-07-12):
   76 migrations aplicadas; scan de TODAS as 85 tabelas referenciadas via `.from()`
   no código × information_schema → criadas `plans` e `resource_permissions`
   (migration 074) que nunca tiveram migration; `get_tenant_id()` ✅; colunas
   críticas ✅. Falso-positivos documentados: `_test_connection` (probe),
   `uploads` (bucket storage), `contracts` (roteia via legacy_docs; export morto).
5. Scripts mortos de Firestore removidos; flake do realtime.service.test endurecido.

## Verificação final
- `tsc --noEmit` (apps/api): **0 erros**
- Suíte backend: **169/169 arquivos, 1337/1337 testes**
- Lição de processo gravada: "code-complete" exige tsc zerado, não só vitest verde.
